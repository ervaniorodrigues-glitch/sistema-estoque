services:
  - type: web
    name: sistema-estoque
    env: python
    buildCommand: "pip install -r requirements.txt && python init_render.py"
    startCommand: "gunicorn -w 4 -b 0.0.0.0:$PORT estoque_web.app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
